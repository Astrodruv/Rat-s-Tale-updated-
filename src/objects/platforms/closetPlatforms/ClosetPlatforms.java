package objects.platforms.closetPlatforms;

import objects.platforms.Platform;
import org.newdawn.slick.Image;
import ui.Images;

public class ClosetPlatforms extends Platform {

    public ClosetPlatforms(float x, float y) {
        super(x, y, Images.closetPlatform);
    }
}
